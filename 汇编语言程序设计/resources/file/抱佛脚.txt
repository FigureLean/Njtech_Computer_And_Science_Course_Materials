寄存器与它们的主要功能：用于寻址的有哪些（段地址，基址、变址，以及它们的对应关系），用于计算的标志位有哪些，数据在内存里的存放示意图
熟悉常用指令的规则（数据传送指令MOV，输入输出指令），能判定指令是否符合语法规则，并纠正错误。
算术运算指令（ADD，SUB，CMP，TEST）对标志位（CF，OF，SF，ZF）的影响，并能得出正确的运算结果。
能写完整的程序（熟练掌握段定义框架），掌握常用的伪指令，理解它们的含义会比较区别（DW，DB，EQU，local），会根据数据定义画出对应的存储器示意图，能计算存储空间的大小（DUP）。
理解并会使用分支指令、循环指令写程序。理解不同分支指令的区别。
会定义和调用子程序。能理解和分析子程序与宏的区别。知道子程序传递参数的不同方式，能分析给定子程序是采用什么方式传递参数的。
熟悉重复汇编指令（IRP/IRPC，&），能写出重复汇编后的程序清单。
会通过21H中断设置和读取中断号，理解中断号和中断向量表的对应关系，会计算给定中断号对应的中断入口程序在中断向量表中存放的地址，并会画对应的示意图。能写出中断指令的执行过程。
另外：会画规范流程图，知道上机操作过程，比如如何生成exe文件，常用的debug指令等等。
课后习题一定要全部理解并掌握！
